[project]
name = "resume-matcher"
version = "0.1.0"
description = "AI-powered resume matching and ranking system"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "python-docx>=1.2.0",
]
